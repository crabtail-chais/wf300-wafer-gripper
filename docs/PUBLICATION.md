# P1D1 公开版范围与历史文档勘误

公开版整理日期：2026-09-13。设计版次仍为 **P1D1**，不是新的机械修订版。

## 未改变的内容

2 个 FCStd、2 个装配 STEP、5 个自制件 STEP、24 个带标注 SVG / DXF、12 页图纸 PDF、11 页装配说明 PDF、装配说明 Markdown 和制造表单均保留原件字节。没有重画、减重或改变尺寸、公差、采购选型及安全门槛。逐项原始和发布哈希见 [provenance.json](../validation/provenance.json)。

FCStd 内历史默认许可证字段未修改，以免破坏其已有哈希绑定；项目权利人的本次显式开源授权及优先适用说明见 [LICENSING](../LICENSING.md)。

## 新增和脱敏

新增公开 README、安全边界、许可、第三方来源说明、贡献指南、只读校验工具和测试。公开范围限于设计、图纸、BOM、装配说明、许可及可移植代码。详细内部测试结果、数值校验报告和历史日志未纳入，文件来源表也不索引这些未公开报告；不改写失败为通过。

## 阅读历史装配说明时的适用说明

原 PDF / Markdown 中关于 `source/`、`evidence/`、`qa/`、旧会话、初次失败截图以及“没有对外发送”的文字描述的是**工程交付时点**，不代表当前仓库的目录、可复现性或发布状态。本说明对公开目录和复现范围的描述优先；机械技术要求未被改写。

| 历史描述 | 公开版实际内容 |
| --- | --- |
| `source/` 可复现流程 | 旧生成器依赖历史会话、本机插件及固定路径，首版不纳入。原生 CAD 是当前设计源文件；不是从零参数重建承诺。 |
| `evidence/`、`qa/` 及初次失败截图 | 不在公开范围；只保留文件来源 / 哈希及验证状态说明。装配说明中的历史检查结论不等于本仓库提供了完整的公开复核证据。 |
| 原始投影 SVG | 仅保留逐页带尺寸图和装配等轴预览，不纳入庞大中间投影数据。 |
| 旧 R2、选型中间版、询价 / 预算材料 | 不属于首个公开基线，避免版本混用和内部商业信息披露。 |

历史几何验证的范围和不能推出的结论见 [VALIDATION](VALIDATION.md)。本次公开 QA 只决定文件是否适合公开，不是机械制造或真实晶圆使用批准。
